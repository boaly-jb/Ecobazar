var mixer = mixitup('.mixer-all');



new VenoBox({
    selector: '.videoHome',
});


     
  $(document).ready(function(){
    $('#slider').slick({
    // dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    adaptiveHeight: true,
    default: 3000,
    });
  });

// $(document).ready(function(){
//     $('.your-class').slick({
//       setting-name: setting-value
//     });
//   });
      